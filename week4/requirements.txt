# Core — needed for run_pipeline.py
anthropic>=0.40.0
python-dotenv>=1.0.0

# Only if you set LLM_PROVIDER=openai
# openai>=1.40.0

# Only if you run langchain_version.py
# langchain>=0.3.0
# langchain-anthropic>=0.3.0
